{
  "name": "netcup",
  "version": "0.2.0",
  "description": "Query currently available netcup voucher codes from the terminal.",
  "license": "MIT",
  "type": "module",
  "packageManager": "bun@1.4.0",
  "bin": {
    "netcup": "./src/bin.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "netcup": "bun src/bin.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run test && bun run typecheck",
    "release:check": "bun pm pack --dry-run",
    "release": "bun publish --access public",
    "prepublishOnly": "bun run check"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/philipp-schuetz/netcup.git"
  },
  "homepage": "https://github.com/philipp-schuetz/netcup#readme",
  "bugs": {
    "url": "https://github.com/philipp-schuetz/netcup/issues"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  },
  "devDependencies": {
    "bun-types": "1.4.0",
    "typescript": "5.9.3"
  }
}
